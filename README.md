# Attendly - Student Attendance System

A comprehensive attendance management system for teachers and students with distinct login portals and dashboards.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
